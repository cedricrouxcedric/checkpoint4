<nav class="bg-light navbar navbar-expand-lg navbar-light text-white-50 ">
    <a class="navbar-brand" href="/">Wild circus</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarText"
            aria-controls="navbarText" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarText">
        <ul class="navbar-nav mr-auto">
            <li class="nav-item active">
                <a class="nav-link" href="{{ path('wilder_index') }}">Wilders <span class="sr-only">(current)</span></a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ path('performance_index') }}">Performance</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ path('type_performance_index') }}">Type de performance</a>
            </li>
            {% if app.user %}
            <li class="nav-item">
                <a class="nav-link" href="{{ path('game') }}">Gagnez votre ticket</a>
            </li>
            {% else %}
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_login') }}">Connectez vous pour tenter de gagner un ticket</a>
                </li>
            {% endif %}
            <li class="nav-item">
                <a class="nav-link" href="{{ path('leaderboard') }}">Leaderboard</a>
            </li>
        </ul>
        <span class="navbar-text">
            {% if app.user %}
                {{ app.user.username }}
                <a href="{{ path('app_logout') }}"> Se déconnecter</a>
{% else %}
                <a href="{{ path('app_login') }}"> Se connecter</a>
            {% endif %}</span>

    </div>

</nav>
{% include 'flash.html.twig' %}
